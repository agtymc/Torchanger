# Screenshots

This directory contains repository screenshots for Torchanger.

- `torchanger000.png` — main application window
- `torchanger001.png` — compact application view

These images are referenced from the repository `README.md` and are also copied into the Debian package documentation directory during `.deb` builds.
